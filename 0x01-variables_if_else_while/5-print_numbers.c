#include <stdio.h>

/**
 * main-the entry point
 * Return:void
 */

int main(void)
{
	int numbers = 0;

	while (numbers < 10)
	{
		printf("%d", numbers);
		numbers++;
	}
	printf("\n");
	return (0);
}
